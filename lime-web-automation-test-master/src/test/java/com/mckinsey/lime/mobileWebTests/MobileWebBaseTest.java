package com.mckinsey.lime.mobileWebTests;

import com.mckinsey.lime.tests.TestSessionInitiator;

class MobileWebBaseTest extends TestSessionInitiator {
/*
    void navigateToLoginPage() {

        LandingPage launchPage = new LandingPage(getDriver());

        launchPage.clickLoginButton();
        new LoginPage(getDriver()).switchToLogInFrame();

        CustomLogging.writeToReport( CustomLogStrings.INFO_LOG + "Navigated to login page");
    }*/

/*    void launchAppToEnglish() {
        new LandingPage(driver).launchApp();

        SideMenu sideMenuPage = new SideMenu(driver);

        sideMenuPage.switchToEnglishLocale();
        CustomLogging.writeInfoLogToReport("switched to English locale");
    }*/
}
